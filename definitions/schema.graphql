type Query {
  articles: [Article]
  article(id: ID!): Article
  authors: [Author]
  author(id: ID!): Author
}

type Article {
  id: ID!
  title: String!
  body: String!
  author: Author
}

type Author {
  id: ID!
  name: String!
  email: String!
  image: String!
  articles: [Article]
}

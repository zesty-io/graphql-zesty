

type Article {
  zuid: ID!
  title: String!
  body: String!
  author: Author!
}

type Author {
  zuid: ID!
  name: String!
  email: String!
  image: String!
  articles: [Article]
}
type Query {
  articles: [Article]
  article(zuid: ID!): Article
  authors: [Author]
  author(zuid: ID!): Author
}